[build]
builder = "nixpacks"

[deploy]
startCommand = "npm start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

# Environment variables
# Note: Set NODE_PATH in Railway dashboard to include node_modules path
# or the backend process will fail to find dependencies
